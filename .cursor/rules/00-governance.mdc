---
alwaysApply: true
---
# Rules Governance（長期維運治理）

## 1. 版本規則
- 採用 `SemVer`：`MAJOR.MINOR.PATCH`。
- `MAJOR`：規則語意變更或破壞相容（例如 Must 規則重寫）。
- `MINOR`：新增可執行規則、檢查清單、失敗訊號。
- `PATCH`：錯字修正、範例修正、描述澄清。

## 2. 變更必備項（缺一不可）
- 必須更新該檔案的 `Version` 與 `Last Updated`。
- 必須新增一條 `Changelog`。
- 必須同時補齊：`成功範例`、`失敗範例`、`失敗訊號`。
- 新增規則若不可檢查，必須補上「最低可執行檢查步驟」。

## 3. 發版前 Gate
- `Gate A`：Pre-flight 8 項全 PASS。
- `Gate B`：命令卡（05-commands）對應檢查清單可逐條執行。
- `Gate C`：至少 1 個「錯誤案例」能被規則準確攔截。

## 4. 退場與棄用
- 規則需棄用時，先標記 `Deprecated`，至少保留 1 個版本週期。
- 同步提供替代規則與遷移方式，禁止直接刪除不告知。

## 5. 變更提案格式
每次提案使用以下模板：

```md
### Change Proposal
- Rule File:
- Current Version:
- Proposed Version:
- Why:
- Success Example:
- Failure Example:
- Fail Signal:
- Verification Steps:
- Breaking Change: Yes/No
```
