---
alwaysApply: true
---
# 架構與元件模板標準

## 規格資訊
- Version：`2.0.0`
- Last Updated：`2026-04-17`
- Priority：`P1 MUST`

## 1. 元件標準結構（Template）
每個 React 元件必須遵循 `[A][B-0][B][C]` 四區塊，順序不可調換：

```tsx
import React from 'react';
import { LAYOUT } from '../../styles/layout';

/**
 * [A] 中文註解：元件角色 + 視覺意圖
 */

// [B-0] 動畫鉤子白名單：只放 selector，不放 UI 樣式
const GSAP_SELECTORS = {
  item: 'work-item-trigger',
} as const;

// [B] UI 樣式常數：Layout → Visual → State → Responsive
const STYLES = {
  wrapper: 'flex flex-col min-h-screen',
  container: LAYOUT.container,
  list: 'grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3',
} as const;

// [C] 資料驅動：重複結構必須 .map()
const ITEMS = [{ id: 'a', label: 'Demo' }];

export const ComponentName: React.FC = () => (
  <section className={STYLES.wrapper}>
    <div className={STYLES.container}>
      <ul className={STYLES.list}>
        {ITEMS.map((item) => (
          <li key={item.id} className={GSAP_SELECTORS.item}>
            {item.label}
          </li>
        ))}
      </ul>
    </div>
  </section>
);
```

## 2. 命名規範
- 結構 key 優先：`wrapper`、`container`、`title`、`description`、`media`、`meta`。
- 只有在衝突時才加語境前綴：`heroTitle`、`cardTitle`。
- selector 命名需可讀且穩定，禁止依視覺文案命名。

## 3. 成功/失敗範例

✅ 成功：`GSAP_SELECTORS` 只承載動畫鉤子，`STYLES` 只承載 UI class。  
❌ 失敗：把 `work-item-trigger` 寫進 `STYLES.item` 並在多處混用。

✅ 成功：列表資料抽 `ITEMS` + `.map()`。  
❌ 失敗：複製貼上 3 份相同 `<li>`。

## 4. 失敗訊號（Fail Signals）
出現任一項即未通過：
- JSX 出現 2 份以上高度相似區塊，卻沒有資料陣列與 `.map()`。
- `GSAP_SELECTORS` 缺席，但有 GSAP 動畫 class。
- `STYLES` key 命名混亂（如 `a1`, `xBlock`）導致不可維護。
- Tailwind class 未依 `Layout → Visual → State → Responsive` 排序。

## 5. TypeScript 穩定性
- 禁止 `// @ts-ignore`、禁止 `any` 逃生。
- `window.gsap` 類型統一定義於 `src/types.ts`。
- CSS 檔同一份只允許單點 import。

### Changelog
- `v2.0.0`：新增規格資訊、成功/失敗範例、Fail Signals 與維運導向檢查。
