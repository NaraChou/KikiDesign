---
globs: *.tsx, *.ts
alwaysApply: false
---
# Tailwind 與 RWD 強制規範

## 1. Tailwind 排序法則 (絕對不可變)
1. **Layout (佈局)**: `flex`, `grid`, `w-`, `h-`, `p-`, `m-`, `absolute`, `z-`
2. **Visual (視覺)**: `bg-`, `text-`, `border-`, `rounded-`, `opacity-`
3. **State (狀態/互動)**: `hover:`, `group-hover:`, `transition-`, `duration-`
4. **Responsive (響應式)**: `md:`, `lg:` **(永遠放在最後面)**

*範例*：`flex items-center w-full bg-black text-white opacity-0 transition-opacity hover:opacity-100 md:flex-row md:justify-between`

## 2. RWD (響應式) 零妥協
- **Mobile First**：預設的 class 都是給手機看的。
- **AI 防呆檢查**：當新增一個並排佈局（如 `flex`）時，AI 必須預設為 `flex-col`，並在 `md:` 才加上 `md:flex-row`。
- **邊距一致**：使用 `layout.ts` 中的 `LAYOUT.container` 統一左右 Padding，禁止在各元件內亂寫 `px-4 md:px-8` 導致不對齊。