---
globs: "*.tsx,*.ts"
alwaysApply: false
---
# Tailwind 與 RWD 強制規範

## 規格資訊
- Version：`2.0.0`
- Last Updated：`2026-04-17`
- Priority：`P1 MUST`

## 1. Tailwind 排序法則（不可變）

```txt
[A] Layout      flex grid w- h- p- m- position z-
[B] Visual      bg- text- border- rounded- shadow- opacity-
[C] State       hover: focus: group-hover: transition- duration-
[D] Responsive  md: lg: xl:（永遠最後）
```

✅ 正確：
```ts
'flex flex-col w-full bg-black text-white transition-opacity hover:opacity-90 md:flex-row md:justify-between lg:gap-16'
```

❌ 錯誤：
```ts
'bg-black flex md:flex-row text-white hover:opacity-90 flex-col'
```

## 2. RWD 零妥協（Mobile First）
- 預設 class 先服務手機。
- 桌機/平板行為必須透過 `md:`、`lg:` 遞增補齊。
- 並排佈局強制規則：
  - ❌ `flex flex-row`
  - ✅ `flex flex-col md:flex-row`
- 版心與水平間距優先使用 `LAYOUT.container`，避免元件各自亂寫。

## 3. 前提與禁止條件
- 前提：只要新增 `flex/grid` 佈局，就必須同步檢查 breakpoint。
- 前提：只要新增 hover/transition，就必須回到 `[C] State` 區。
- 禁止：在同一 class 字串混入無序斷點（`lg:` 在 `md:` 前且語義衝突）。
- 禁止：用 JS 事件改變純視覺 hover 狀態（改用 CSS/Tailwind）。

## 4. 失敗訊號（Fail Signals）
- 出現 `flex-row`，但同區塊沒有 base `flex-col`。
- 響應式 class 與基礎 class 混序，導致閱讀與維護困難。
- 同頁多區塊左右邊距不一致（未使用統一 container token）。
- hover 視覺以 `onMouseOver` 實作且無必要業務邏輯。

## 5. 最低可執行檢查
- 檢查所有 `flex` 佈局：是否滿足 `base=col, md=row`。
- 檢查 Tailwind 字串順序：是否符合 `A→B→C→D`。
- 檢查同頁 section：水平邊距是否一致。

### Changelog
- `v2.0.0`：新增錯誤範例、前提/禁止條件、Fail Signals 與最低可執行檢查。
