---
globs: "*.tsx,*.ts"
alwaysApply: false
---
# Motion V5 Standard — GSAP 與 rAF 效能規範

## 規格資訊
- Version：`2.0.0`
- Last Updated：`2026-04-17`
- Priority：`P0 MUST`

## 1. Demand-Driven rAF（休眠/喚醒雙旗標）
必須實作 `sleeping` + `rafActive`，避免永久迴圈：

```ts
const sleeping = useRef(false);
const rafActive = useRef(false);
const rafId = useRef<number>(0);

const loop = () => {
  const dx = target.x - current.x;
  const dy = target.y - current.y;

  current.x += dx * 0.15;
  current.y += dy * 0.15;

  if (Math.abs(dx) < 0.1 && Math.abs(dy) < 0.1) {
    sleeping.current = true;
    rafActive.current = false;
    return;
  }

  rafId.current = requestAnimationFrame(loop);
};
```

## 2. useEffect 清理（即使自停也必須強停）
`requestAnimationFrame` 有自我停止邏輯，仍不能省略 cleanup 強制終止：

```ts
useEffect(() => {
  let ctx: ReturnType<typeof window.gsap.context> | null = null;

  ctx = window.gsap.context(() => {
    // gsap setup
  }, rootRef);

  return () => {
    ctx?.revert();
    cancelAnimationFrame(rafId.current);
    window.removeEventListener('mousemove', handleMouseMove);
  };
}, []);
```

## 3. GPU 優先輸出
✅ 正確：`transform: translate3d(...) translate(-50%, -50%)`。  
❌ 錯誤：頻繁改 `top/left` 造成 reflow。

## 4. ResizeObserver 安全模式（四步 Pipeline）
- Step 1：`disconnect()`
- Step 2：`ScrollTrigger.refresh()`
- Step 3：更新穩定高度快照
- Step 4：`requestAnimationFrame(() => observe())`

且必須包含：
- `debounce = 150ms`
- `threshold = 2px`
- callback 內禁止直接 `refresh()`

## 5. 成功/失敗範例
✅ 成功：含雙旗標、靜止判定、完整 cleanup、RO 四步流程。  
❌ 失敗：僅寫 `requestAnimationFrame(loop)` 並在 unmount 時不清理。

## 6. 失敗訊號（Fail Signals）
- 沒有 `Math.abs(dx/dy)` 靜止判定。
- 缺 `cancelAnimationFrame` 或 `ctx?.revert()` 任一項。
- `ctx` 宣告在 `setTimeout` 等區塊內導致 cleanup 拿不到。
- ResizeObserver callback 直接呼叫 `ScrollTrigger.refresh()`。
- 以 `top/left` 作為高頻動畫主通道。

## 7. 最低可執行檢查
- 搜尋 `requestAnimationFrame`：是否都能找到對應停止條件與 cleanup。
- 搜尋 `ResizeObserver`：是否包含 `disconnect → refresh → rAF observe`。
- 搜尋動畫位移：是否用 `translate3d` 為主。

### Changelog
- `v2.0.0`：新增失敗訊號、最低可執行檢查、cleanup 強制終止理由與四步 Pipeline 固化。
