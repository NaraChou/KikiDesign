---
globs: "*.tsx"
alwaysApply: false
---
# SEO 與語意化佈局防線

## 規格資訊
- Version：`2.0.0`
- Last Updated：`2026-04-17`
- Priority：`P1 MUST`

## 1. 語意化標籤（禁止全 div）
- `<main>`：每頁主要內容唯一容器。
- `<nav>`：導覽區（主選單、麵包屑）。
- `<section>`：主題區塊，需有對應標題。
- `<article>`：可獨立理解的內容單元。
- `<h1>`：每頁唯一主標題；`h2/h3` 不可跳層。
- `<button>`：觸發操作；圖示按鈕必填 `aria-label`。
- `<a>`：導向連結；文案需具體，不用「點擊這裡」。

## 2. 圖片屬性用途與必填規則
| 屬性 | 是否必填 | 用途 |
|---|---|---|
| `alt` | 必填 | 提供可及性文字與語意資訊 |
| `width` / `height` | 必填 | 預留版位，降低 CLS |
| `aspect-*` | 必填 | 鎖定視覺比例，防拉伸 |
| `loading` | 必填 | 首屏 eager，非首屏 lazy |
| `fetchPriority` | 條件必填 | 首屏主圖建議 `high` |
| `decoding="async"` | 建議 | 降低主執行緒阻塞 |

## 3. index.html 必填 meta 清單
以下項目預設必備：

```html
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>...</title>
<meta name="description" content="..." />
<meta name="robots" content="index,follow" />
<link rel="canonical" href="https://yourdomain/..." />

<meta property="og:type" content="website" />
<meta property="og:title" content="..." />
<meta property="og:description" content="..." />
<meta property="og:url" content="https://yourdomain/..." />
<meta property="og:image" content="https://yourdomain/og-image.png" />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="..." />
<meta name="twitter:description" content="..." />
<meta name="twitter:image" content="https://yourdomain/og-image.png" />
```

## 4. 失敗訊號（Fail Signals）
- 一個頁面出現多個 `<h1>` 或標題層級跳躍。
- 大量主體內容落在無語意 `<div>`，缺 `main/section/article`。
- 圖片缺 `alt`、`width/height` 或 `aspect-*` 任一項。
- 首屏圖未標記 `loading="eager"` 且缺高優先載入策略。
- `index.html` 缺 `description`、`canonical` 或主要 OG/Twitter meta。

## 5. 最低可執行檢查
- 檢查 heading 階層是否連續。
- 檢查每張圖是否具備 CLS 防線四件組。
- 檢查首頁 meta 是否齊全，分享卡是否可用。

### Changelog
- `v2.0.0`：補齊完整 meta 清單、圖片屬性用途表、Fail Signals 與可執行檢查。
