---
alwaysApply: true
---
# Kiki 專屬 AI 指令執行手冊

## 規格資訊
- Version：`2.0.0`
- Last Updated：`2026-04-17`
- 原則：每個指令都必須有「執行前提、逐條檢查、阻斷條件、輸出格式」。

## `/FormatKiki` — 結構與樣式格式化

### 執行前提
- 當前檔案包含 JSX/Tailwind class。

### 逐條檢查
- [ ] Tailwind class 重排為 `Layout → Visual → State → Responsive`。
- [ ] 所有並排佈局確認 `flex-col` base + `md:flex-row`。
- [ ] JSX 字串樣式抽離到 `STYLES` 常數（保留必要動態 class）。
- [ ] 動畫 selector 從 `STYLES` 拆回 `GSAP_SELECTORS`。

### 阻斷條件（Fail）
- 發現 `flex-row` 無 `flex-col` base。
- `STYLES` 與 `GSAP_SELECTORS` 仍混寫。

### 輸出格式
```txt
[FormatKiki]
TAILWIND_ORDER: PASS|FAIL
MOBILE_FIRST: PASS|FAIL
STYLES_EXTRACTION: PASS|FAIL
SELECTOR_SEPARATION: PASS|FAIL
VERDICT: PASS|FAIL
```

## `/CheckCLS` — 視覺穩定性檢查

### 執行前提
- 當前檔案有 `<img>`、`<picture>` 或背景主視覺。

### 逐條檢查
- [ ] 列出所有圖片元素。
- [ ] 每張圖具備 `alt`、`width`、`height`、`aspect-*`。
- [ ] 首屏圖為 `loading="eager"` + `fetchPriority="high"`。
- [ ] 非首屏圖為 `loading="lazy"`。
- [ ] `alt` 為具體描述，非「圖片/圖示」。

### 阻斷條件（Fail）
- 任一圖片缺少 CLS 四件組。
- 首屏圖未使用高優先策略。

### 輸出格式
```txt
[CheckCLS]
IMAGE_INVENTORY: PASS|FAIL
CLS_GUARD: PASS|FAIL
LCP_PRIORITY: PASS|FAIL
ALT_QUALITY: PASS|FAIL
VERDICT: PASS|FAIL
```

## `/AuditV5` — Motion V5 稽核

### 執行前提
- 檔案含 GSAP、`requestAnimationFrame`、ResizeObserver 其一。

### 逐條檢查
- [ ] rAF 有靜止判定與雙旗標。
- [ ] cleanup 含 `ctx?.revert()` + `cancelAnimationFrame` + `removeEventListener`。
- [ ] 位移以 `translate3d` 為主，避免 `top/left` 高頻更新。
- [ ] ResizeObserver 使用 `disconnect → refresh → rAF observe`。
- [ ] ResizeObserver 有 `debounce + threshold`。

### 阻斷條件（Fail）
- callback 內直接 `ScrollTrigger.refresh()`。
- rAF 缺停止條件或缺 cleanup。

### 輸出格式
```txt
[AuditV5]
RAF_SLEEP: PASS|FAIL
CLEANUP_COMPLETE: PASS|FAIL
GPU_PATH: PASS|FAIL
RO_SAFE_PIPELINE: PASS|FAIL
VERDICT: PASS|FAIL
```

## `/AuditSEO` — SEO 與語意化稽核

### 執行前提
- 當前檔案是頁面或主要區塊結構。

### 逐條檢查
- [ ] 是否有 `main/nav/section/article` 正確落位。
- [ ] `h1~h3` 階層是否連續且每頁單一 `h1`。
- [ ] `<a>` 文案是否具體可理解。
- [ ] `<button>`（尤其圖示按鈕）是否具備 `aria-label`。
- [ ] `index.html` meta 是否符合 04 規範。

### 阻斷條件（Fail）
- 結構只用 `<div>` 堆疊主要內容。
- 標題層級斷裂或多 `h1`。

### 輸出格式
```txt
[AuditSEO]
SEMANTIC_STRUCTURE: PASS|FAIL
HEADING_HIERARCHY: PASS|FAIL
LINK_ACCESSIBILITY: PASS|FAIL
BUTTON_ACCESSIBILITY: PASS|FAIL
META_COMPLETENESS: PASS|FAIL
VERDICT: PASS|FAIL
```

## `/Preflight` — 最終交付總閘門

### 執行前提
- 準備交付 PR 或完整功能段落前。

### 逐條檢查
- [ ] 套用 `.cursorrules` 的 8 項 Pre-flight。
- [ ] 所有相關命令卡結果均為 PASS。
- [ ] 若有 FAIL，列出阻塞項與修復結果。

### 阻斷條件（Fail）
- 任一 Must 規則失敗仍嘗試交付。

### 輸出格式
```txt
[PRE-FLIGHT]
RWD_MOBILE_FIRST: PASS|FAIL
TAILWIND_ORDER: PASS|FAIL
DRY_MAP_RENDER: PASS|FAIL
CLS_IMAGE_GUARD: PASS|FAIL
RAF_SLEEP_FLAGS: PASS|FAIL
EFFECT_CLEANUP: PASS|FAIL
SEMANTIC_HTML: PASS|FAIL
GSAP_STYLE_SEPARATION: PASS|FAIL
VERDICT: PASS|FAIL
BLOCKERS: ...
```

### Changelog
- `v2.0.0`：所有命令新增執行前提、阻斷條件與標準化輸出，並新增 `/Preflight` 終閘門。
