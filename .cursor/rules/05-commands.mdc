---
alwaysApply: true
---
# Kiki 專屬 AI 快捷指令庫 (Commands)

當使用者在對話框輸入以下 `#自訂指令` 時，請嚴格執行對應的檢查與任務：

## 🛠️ 效能與架構審計指令

### `/AuditV5` (執行 Motion V5 效能審計)
當觸發此指令時，AI 必須掃描當前檔案並回答：
1. 所有的 `requestAnimationFrame` 是否都有 `Math.abs(dx) < 0.1` 的自動休眠機制？
2. `useEffect` 中是否有遺漏的 `cancelAnimationFrame` 或 `ctx.revert()`？
3. 動畫位移是否都使用了 `translate3d` (GPU 加速) 而非改變 `top/left`？

### `/CheckCLS` (執行視覺穩定性防禦檢查)
當觸發此指令時，AI 必須掃描當前元件的 HTML/JSX：
1. 列出所有 `<img>` 標籤。
2. 檢查是否每個圖片都有 `aspect-ratio` (或 Tailwind 的 `aspect-`) 保護。
3. 檢查是否都有明確的 `width` 與 `height` 或 `object-cover` 屬性。

### `/FormatKiki` (執行 Kiki Style 程式碼格式化)
當觸發此指令時，AI 必須自動重構當前檔案：
1. 將 Tailwind Class 嚴格依照 `Layout -> Visual -> State -> Responsive` 重新排序。
2. 檢查 RWD 斷點，補齊遺漏的 `md:` 與 `lg:`。
3. 把散落的字串樣式抽離到 `STYLES` 常數物件中。

### `/AuditSEO` (執行 SEO 與語意化架構審計)
當觸發此指令時，AI 必須以搜尋引擎爬蟲的視角掃描當前檔案：
1. **語意標籤**：檢查是否過度使用 `<div>`？主要內容是否有 `<main>`、`<article>`？導覽是否有 `<nav>`？
2. **標題階層**：檢查 `<h1>` 到 `<h6>` 的使用是否符合邏輯結構，沒有斷層。
3. **可及性 (a11y)**：檢查所有的 `<a>` 是否有具體的文字內容（禁用「點擊這裡」），所有的 `<button>` 是否有 `aria-label`。